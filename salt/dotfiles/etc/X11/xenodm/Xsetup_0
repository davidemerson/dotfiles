# black X11 blackground
${exec_prefix}/bin/xsetroot -solid black

# turn the damn beeps off
${exec_prefix}/bin/xset b off
${exec_prefix}/bin/xset b 0 0 0

# behold, the field where where I launch the default console window, you can see it is barren